import { EdgeLoc, VertexLoc } from "./constants";

class Utils {
    static getEdgeNeighbour(curr: BoardCoord, direction: EdgeLoc): BoardCoord {
        switch (direction) {

        }
        return new BoardCoord(0, 0);
    }
}